name: github-repo-stats

# PAUSED: inherited from the upstream fork. The daily schedule is disabled;
# run manually only. To re-enable, restore:
#   schedule:
#     - cron: "50 23 * * *"
on:
  workflow_dispatch: # Allow for running this manually.

jobs:
  j1:
    name: github-repo-stats
    runs-on: ubuntu-latest
    steps:
      - name: run-ghrs
        # Use latest release.
        uses: jgehrcke/github-repo-stats@RELEASE
        with:
          ghtoken: ${{ secrets.ghrs_github_api_token }}

